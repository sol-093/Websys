The navbar logo is called inline from `src/core/layout.php`.

Current files:

- Light mode: `public/uploads/logolight.png`
- Dark mode: `public/uploads/logodark.png`

Change logo sizing in the `.nav-logo` and `.nav-logo-img` CSS rules in `src/core/layout.php`.
